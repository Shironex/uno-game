{
  "name": "uno-app",
  "version": "1.0.1",
  "scripts": {
    "build": "cd Server && yarn && yarn build",
    "start": "cd Client && yarn build --outDir ../Server/client/build && cd Server && yarn start",
    "heroku": "heroku logs -a uno-game --tail"
  },
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": ">=16.x",
    "yarn": ">=1.22.19"
  }
}