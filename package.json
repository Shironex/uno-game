{
  "name": "uno-app",
  "version": "1.0.1",
  "scripts": {
    "build": "cd Server && yarn && yarn build",
    "start": "cd Server && yarn start"
  },
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": ">=16.x",
    "yarn": ">=1.22.19"
  }
}